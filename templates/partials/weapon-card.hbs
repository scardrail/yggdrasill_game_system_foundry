<div class="weapon-card">
    <div class="weapon-info weapon-name">
        <p>{{name}}</p>
    </div>
    <div class="weapon-info weapon-stats">
        <p>{{data.dmgBase}}/{{data.solidity}}/{{data.enc}}/{{data.price}}</p>
    </div>
</div>